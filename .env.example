# ELEZON — frontend env. Copy to `.env` and fill in.
# Supabase → Project Settings → API. The anon key is public-safe (RLS-protected).
VITE_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR-ANON-PUBLIC-KEY
